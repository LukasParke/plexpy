# Scope

The value `all` is the only supported `scope` parameter.

## Example Usage

```python
from plex_api_client.models.operations import Scope

value = Scope.ALL
```


## Values

| Name  | Value |
| ----- | ----- |
| `ALL` | all   |